{
  "id": "openrouter",
  "name": "OpenRouter",
  "type": "cloud",
  "website": "https://openrouter.ai",
  "logoPath": "/openrouter.svg",
  "supportsEmbeddings": true,
  "supportsChat": true,
  "supportsTextGeneration": true,
  "supportsExploration": true,
  "requiresApiKey": true,
  "requiresBaseUrl": false,
  "embeddingModels": [
    {
      "id": "google/gemini-embedding-2",
      "name": "Gemini Embedding 2",
      "description": "Google's current embedding model (May 2026), 3072 dimensions, 8K context. Through OpenRouter its DEFAULT output is the semantic-similarity space -- byte-identical to input_type: semantic_similarity -- while search_query and search_document are distinct. Symmetric by default, which is what recommendations and related-titles want.",
      "capabilities": {
        "supportsToolCalling": false,
        "supportsToolStreaming": false,
        "supportsObjectGeneration": false,
        "supportsEmbeddings": true
      },
      "embeddingDimensions": 3072,
      "quality": "standard",
      "costTier": "low",
      "inputTypeNote": "No mode needed. Measured through OpenRouter, its default output is byte-identical to semantic_similarity (max coordinate difference 0.0), which is the symmetric space this app wants. Setting it explicitly is harmless and documents the intent, but changes nothing."
    },
    {
      "id": "google/gemini-embedding-001",
      "name": "Gemini Embedding 001",
      "description": "Previous-generation Google embedding model, 3072 dimensions. Through OpenRouter its default output is the RETRIEVAL space: default, search_query and search_document all return the identical vector, and only input_type: semantic_similarity differs (measured cosine 0.867 on the same text). Set the retrieval mode explicitly for item-to-item work.",
      "capabilities": {
        "supportsToolCalling": false,
        "supportsToolStreaming": false,
        "supportsObjectGeneration": false,
        "supportsEmbeddings": true
      },
      "embeddingDimensions": 3072,
      "quality": "standard",
      "costTier": "low",
      "recommendedInputType": "semantic_similarity",
      "inputTypeNote": "Its default output is the RETRIEVAL space. Measured through OpenRouter, default, search_query and search_document all return the identical vector, and only semantic_similarity differs (cosine 0.867 on the same text). Recommendations compare films against other films and build a taste vector by averaging them, so this needs the symmetric space set explicitly."
    },
    {
      "id": "qwen/qwen3-embedding-8b",
      "name": "Qwen3 Embedding 8B",
      "description": "Open-weights embedder, 4096 dimensions, 32K context. Instruction-aware, but its documented recipe puts the instruction on the QUERY side against bare documents -- there is no official symmetric recipe, so it does not take a retrieval mode here. OpenRouter routes it across several upstream providers unless routing is pinned.",
      "capabilities": {
        "supportsToolCalling": false,
        "supportsToolStreaming": false,
        "supportsObjectGeneration": false,
        "supportsEmbeddings": true
      },
      "embeddingDimensions": 4096,
      "quality": "standard",
      "costTier": "low",
      "inputTypeNote": "Takes no mode. Qwen is instruction-aware, but its documented recipe puts the instruction on the QUERY against bare documents, and this recommender has no text query -- the query is a centroid of item vectors. Prefixing every document identically is not an official Qwen form."
    }
  ],
  "chatModels": [],
  "textGenerationModels": [],
  "explorationModels": []
}
